ll val;
ll idx = lower_bound(v2.begin(), v2.end(), val) - v2.begin();
ll value = lower_bound(v2.begin(), v2.end(), val);

// lower bound of a value: first value that is greater or equal
// 2 4 5 6 6 7           // lower bound value for 3: 4        lower value index for 3: 1
                        //  lower bound value for 6: 6        lower value index for 6: 3

// upper bound of a value: first value that is stricly greater
// 2 4 5 6 6 7         // upper bound value for 6: 7          upper value index for 6: 4



// 2 4 6 6 6 6 8 9   // for value 6 lower bound index = 2   // value=6
                    //  for value 6 upper bound index = 6   // value=8


### so if any value present multiple time then if
i) we need all value then we need to go to right   // so need upper bound
ii) we need just one first value then stay on left    // so need lower bound 
