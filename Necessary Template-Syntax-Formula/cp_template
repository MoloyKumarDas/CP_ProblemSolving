#include <bits/stdc++.h>
using namespace std;
#define fastio                    \
    ios_base::sync_with_stdio(0); \
    cin.tie(NULL)
#define ll long long
#define vl vector<ll>
#define ff first
#define ss second
#define mod1(sum) sum = (sum % M + M) % M;
#define yes cout << "YES" << endl;
#define no cout << "NO" << endl;
#define sort1(v) sort(v.begin(), v.end());
#define sort2(v) sort(v.begin(), v.end(), [](const pair<ll, ll> &a, const pair<ll, ll> &b) { return a.second < b.second; });
#define reverse1(v) reverse(v.begin(), v.end());
#define decreasing(v) sort(v.begin(), v.end(), greater<>());
#define checkBit(n, i) (n & 1ll << i) // for larger term  (1ll<<i) is safe than (1<<i)
#define setBit(n, i) (n |= 1ll << i)
const ll M = 1e9 + 7;

void solve()
{
    // ll n;
    // cin >> n;

    // ll odd = 0, even = 0,found=0;
    // ll sum = 0,ans=0,cnt=0,cnt1 = 0,cnt2=0;

    // ll a[n + 1];
    // for (ll i = 1; i <= n; i++)
    // {
    //     cin >> a[i];
    // }

    // vl v1(n);
    // for (auto &d : v1)
    // {
    //     cin >> d;
    // }

    /*--------------------------------------------------------------------*/




}
int main()
{
    fastio;
    ll t;
    cin >> t;
    while (t--)
    {
        solve();
        cout << endl;
    }
    return 0; // ha ha ha
}
